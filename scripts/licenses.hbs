# Third-party licenses

Generated with cargo-about 0.9.2 from Cargo.lock. This is the union of the
workspace's default-feature runtime and build dependencies on all platforms;
individual wheels and binaries use a subset. htmlarc's own license is included
for completeness. Native dependency notices follow in THIRD_PARTY_NATIVE.txt.
Regenerate with the commands in docs/releasing.md.

{{#each licenses}}
## {{name}} ({{id}})

Used by:
{{#each used_by}}
- {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}} — {{{crate.repository}}}{{/if}}
{{/each}}

```text
{{{text}}}
```

{{/each}}
